{
  "name": "news-maker",
  "version": "0.1.0",
  "description": "Generador de noticias en formato Cabronazi (1080x1350) para Facebook",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
